# Modèles orphelins — aucune table physique (Étape 6)

Déclarés dans le code COSOMIS, aucune table nulle part. À retirer (ou `managed=False` + jamais migrés).

- `authentication_governmentworker` — modules : authentication.models
- `authentication_user` — modules : authentication.models
- `authentication_user_groups` — modules : authentication.models
- `authentication_user_user_permissions` — modules : authentication.models
